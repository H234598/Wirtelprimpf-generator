# Optionenregister

Das verbindliche Optionenregister liegt in
[`WEB-OPTIONS.md`](WEB-OPTIONS.md). Diese Datei hält den im Bauplan benannten
deutschen Dokumentpfad stabil; die Entscheidung und die Tests liegen an einer
Stelle.
