# ADR-002: Pages fuer Site, Releases fuer Originale

## Entscheidung

Die statische Website wird als kleines geprüftes Pages-Artefakt veröffentlicht.
Originale und Webderivate bleiben releasegebunden außerhalb des Git-Hauptbaums;
die Website verwendet nur hashgebundene öffentliche Download-URLs.

## Begründung

So bleiben Pages-Transfer, Git-Baumwachstum und releasegebundener Medienumfang
getrennt messbar. Der Pages-Validator blockiert Originalquellen, fremde Runtime-
Requests, unsichere Dateien und überschrittene Budgets.

## Grenzen und Rückbau

Warn- und Blockschwellen stehen in `config/web-budgets.json` und den Hosting-
Messberichten. Ein Wechsel der Strategie benötigt eine neue Messung, eine
Provenienzentscheidung und einen reversiblen Release; lokale Status- und
Diagnoseberichte werden nie in `web/dist` kopiert.

Diese Datei projiziert ADR-WEB-002/003 des kanonischen Plans. Eine externe
Pages- oder Hostingfreigabe wird damit nicht behauptet.

## Aktueller lokaler Nachweis am 5. August 2026

Der erfolgreiche read-only Dreifachlauf mit `SOURCE_DATE_EPOCH=0` bestand mit
Laufzeiten von `14,637 s`, `16,476 s` und `15,664 s` (Median `15,664 s`, P95
`16,395 s`) und maximal `568.072 KiB` Kindprozess-RSS. Das validierte
Pages-Artefakt umfasst `1.036` Dateien, `1.013` HTML-Dateien, `21.910.908`
Bytes und `59.820` interne Links; Budgetentscheidung und Arbeitskopieprüfung
sind grün. Der Treehash lautet
`23748a6549e671074b3ee60d98a28dd1596922e97471d38e0a4d8e9fd370ec61`.

Die Quelle umfasst `779` Medien in vier Shards mit `3.654.670.091`
Originalbytes; das Pages-/Release-Verhältnis beträgt `0,0059953`. Die
Wachstumshistorie bleibt wegen eines belastbaren Manifestpunkts
`insufficient_history`. Diese Werte aktualisieren den lokalen Nachweis, aber
noch nicht die Produktionsentscheidung: Factory-Repin, Rechteprüfung,
externe Pages-/DNS-Abnahme, Review und Rollback bleiben offen.
